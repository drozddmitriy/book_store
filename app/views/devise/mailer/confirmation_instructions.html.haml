%p
  Welcome #{@email}!
%p You can confirm your account email through the link below:
%p= link_to I18n.t('views.devise.registrations.mail_confirm'), confirmation_url(@resource, confirmation_token: @token)
