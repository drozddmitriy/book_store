%p
  = t('views.devise.registrations.welcome', email: @email)
%p
  = t('views.devise.registrations.confirm_account_text')
%p= link_to t('views.devise.registrations.mail_confirm'), confirmation_url(@resource, confirmation_token: @token)
